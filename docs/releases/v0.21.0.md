# Release v0.21.0 — proven invocations

Date: 2026-09-26
Follows: v0.20.0 (correctness and docs)

## Included (Steps 194–249)

- Every documented command actually runs: Argo stage runner, backup entrypoint,
  and both DVC module invocations proven via subprocess tests.
- Pipeline CLIs exist where dvc.yaml always said they did; quickstart runs them.

## Verification

`python -m pytest -q` → 175 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema + seam ready, driver pending); compose never run here;
  R3 excluded.

## Tag

`v0.21.0` on `main`. Create the GitHub Release from this tag in the web UI.
