# Release v0.9.0 — release engineering

Date: 2026-09-25
Follows: v0.8.0 (full platform manifests)

## Included (Steps 107–109)

- First-party images advance to v0.8.0 in one atomic bump (uniformity tested).
- Tag policy documents the enforced uniform-bump rule.

## Verification

`python -m pytest -q` → 131 passed; `npm run check` + `build` green.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores; R3 excluded.

## Tag

`v0.9.0` on `main`. Create the GitHub Release from this tag in the web UI.
