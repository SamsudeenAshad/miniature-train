# Release v0.25.0 — contracts and guides

Date: 2026-09-27
Follows: v0.24.0 (docs true and API locked down)

## Included (Steps 224–289)

- Inference earns a versioned contract with parity tests; schemas gain
  snapshot and model-card contracts with live conformance.
- Handover, operator guide, runbook, and evidence refreshed to the real system.
- API: anonymous/role denial, request IDs, replay codes, envelope errors.

## Verification

`python -m pytest -q` → 183 passed; `npm test`/`typecheck`/`build`, `pip-audit`/`npm audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema + seam ready, driver pending); compose never run here;
  R3 excluded.

## Tag

`v0.25.0` on `main`. Create the GitHub Release from this tag in the web UI.
