# Release v0.16.0 — contracts honed

Date: 2026-09-26
Follows: v0.15.0 (governance refresh and local-dev hardening)

## Included (Steps 194–199)

- Correct replay status codes (200 vs 201) documented in OpenAPI.
- Response schemas with live conformance tests: predictions, dataset manifests,
  snapshot manifests, model cards.

## Verification

`python -m pytest -q` → 165 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema ready, driver pending); compose never run here; R3 excluded.

## Tag

`v0.16.0` on `main`. Create the GitHub Release from this tag in the web UI.
