# Release v0.10.0 — enforced APIs and console wiring

Date: 2026-09-25
Follows: v0.9.0 (release engineering)

## Included (Steps 111–119)

- Console: runtime backend config, nginx proxy to services, ConfigMap mounting.
- Control API: membership enforcement with concealment, Idempotency-Key with 409s.
- Contracts: route parity tests against implementation.
- Fleet sweeps: ConfigMap refs, secret refs, selectors (incl. PDBs/monitors/Rollout).

## Verification

`python -m pytest -q` → 138 passed; `npm run check` + `build` green.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores; R3 excluded.

## Tag

`v0.10.0` on `main`. Create the GitHub Release from this tag in the web UI.
