# Release v0.3.0 — deployment and detection depth

Date: 2026-09-24
Follows: v0.2.0 (governance, risk review, Kubernetes start)

## Included (Steps 30–39)

- Kubernetes: control-api deployment/service, Argo Rollout canary + AnalysisTemplate, Argo CD app with field ownership, training WorkflowTemplate.
- Telemetry: OTel collector (redaction, Prometheus/Loki), Prometheus rules, Alertmanager grouping/inhibition.
- Detection: Evidently adapter config, frozen fault harness (30 episodes), Isolation Forest challenger (unavailable without sklearn; baseline retained).
- Supply chain: artifact trust gates, SBOM/provenance records.

## Verification

`python -m pytest -q` → 74 passed. All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; R3 excluded.

## Tag

`v0.3.0` on `main`. Create the GitHub Release from this tag in the web UI.
