# Release v0.19.0 — local stack and API polish

Date: 2026-09-26
Follows: v0.18.0 (seams and unification)

## Included (Steps 184–229)

- Compose: healthchecks, healthy-only ordering, restart policies, documented stack.
- APIs: envelope errors everywhere, Location headers, membership concealment,
  versioned services and contracts in parity.
- Docs: operator guide, runbook, handover, and governance refreshed.

## Verification

`python -m pytest -q` → 169 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema + seam ready, driver pending); compose never run here;
  R3 excluded.

## Tag

`v0.19.0` on `main`. Create the GitHub Release from this tag in the web UI.
