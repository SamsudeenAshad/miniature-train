# Release v0.18.0 — seams and unification

Date: 2026-09-26
Follows: v0.17.0 (API correctness)

## Included (Steps 194–219)

- Dead fields removed; private-store reaches closed behind public functions.
- Predictor loads once; health endpoints report real backend state.
- Contract and service versions advance together (control 0.2.0).
- Magic numbers unified: scheduler quota, executor timing, drift bounds all read
  from versioned policy/adapter files.

## Verification

`python -m pytest -q` → 168 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema + seam ready, driver pending); compose never run here;
  R3 excluded.

## Tag

`v0.18.0` on `main`. Create the GitHub Release from this tag in the web UI.
