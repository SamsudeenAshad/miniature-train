# Release v0.1.0 — R1 skeleton + R2 policy slices

Date: 2026-09-24
Baseline: MT-SRS-WBS-001 v1.0 (proposed; numbers are targets, not measured results)

## Included (Steps 1–27, 64 tests green)

- Reproducible data: seeded demand generator (20160 rows, time-ordered splits + 1h purge), validation/quarantine, immutable snapshots, DVC stages.
- Features/training: versioned features with offline/online parity, deterministic baselines + promo-lift candidate, quality gates, registry with pinned aliases, bounded tuning, model cards, eval bundle, label joins.
- Serving/delivery: schema-validated inference, stage/shadow/approve/promote/rollback, canary guards, enrolled abort, Git-state reconciliation, training scheduling.
- AIOps: telemetry health + redaction, drift/retraining eligibility, statistical detector, correlation, hypothesis ranking, benchmark scoring, capacity bounds, topology expiry.
- Recovery/governance: runbook dry-run, execution policy (15-min approval, 2-min evidence, cooldown, kill switch), fenced idempotent executor, authz, audit chain, restore/retention, outbox dedup, API errors/idempotency, connector SSRF guard.
- Handover: operator guide, acceptance index AT-01..AT-27, evidence index, console state contract.

## Verification

`python -m pytest -q` → 64 passed on Python 3.13. All commits by `samsudeenashad`, no co-author trailers.

## Known limitations (not claims)

- Perf/SLO figures (NFR-001..007, 30-day 99.5%) unmeasured; no cluster load run yet (AT-21 deferred).
- No Kubernetes/Argo/Istio deployment yet; Docker Compose + health stubs only.
- Learned detector is statistical baseline; Isolation Forest challenger pending.
- R3 copilot excluded.

## Tag

`v0.1.0` on `main`. Create the GitHub Release from this tag in the web UI.
