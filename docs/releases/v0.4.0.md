# Release v0.4.0 — hardening and GitOps depth

Date: 2026-09-24
Follows: v0.3.0 (deployment and detection depth)

## Included (Steps 40–43)

- Backup: daily CronJob (RPO 24h) + restore runbook (RTO 2h target).
- Hardening: non-root read-only containers, scoped service account, default-deny NetworkPolicy, ExternalSecret references.

## Verification

`python -m pytest -q` → 78 passed. All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; R3 excluded.

## Tag

`v0.4.0` on `main`. Create the GitHub Release from this tag in the web UI.
