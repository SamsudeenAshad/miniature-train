# Release v0.22.0 — follow-through

Date: 2026-09-26
Follows: v0.21.0 (proven invocations)

## Included (Steps 251–253)

- First-party images advance to v0.21.0 with uniformity enforcement.
- Evidence, change log, risks, and acceptance decisions track the release.

## Verification

`python -m pytest -q` → 175 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema + seam ready, driver pending); compose never run here;
  R3 excluded.

## Tag

`v0.22.0` on `main`. Create the GitHub Release from this tag in the web UI.
