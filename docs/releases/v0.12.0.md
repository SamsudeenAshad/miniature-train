# Release v0.12.0 — seams and supply chain

Date: 2026-09-26
Follows: v0.11.0 (verified wiring)

## Included (Steps 152–159)

- Storage seam: routes isolated behind a replaceable backend (Postgres path open).
- Runbook: migration rule, fixed numbering, disruption budgets.
- Web: version tracking, strict typecheck, backend status, config checks, nginx proxy.
- Supply chain: pip-audit clean (pytest/starlette patched), vite patched, httpx2,
  CI audits, supply-chain policy, release alignment tests.

## Verification

`python -m pytest -q` → 160 passed; `tsc`/`check`/`build`/`pip-audit`/`npm audit` reviewed.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores; R3 excluded.

## Tag

`v0.12.0` on `main`. Create the GitHub Release from this tag in the web UI.
