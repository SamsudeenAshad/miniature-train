# Release v0.11.0 — verified wiring

Date: 2026-09-25
Follows: v0.10.0 (enforced APIs and console wiring)

## Included (Steps 111–129)

- Console: mounted runtime config, nginx proxy, web image, deploy.
- Fleet sweeps: ConfigMap refs, secret refs, selectors (services/PDBs/monitors/Rollout),
  volumes, tokens, seccomp, resources, probes, image uniformity.
- Docs kept honest: evidence index, platform status, runbook, acceptance coverage of all files.

## Verification

`python -m pytest -q` → 141 passed; `npm run check` + `build` green.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores; R3 excluded.

## Tag

`v0.11.0` on `main`. Create the GitHub Release from this tag in the web UI.
