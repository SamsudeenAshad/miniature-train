# Release v0.20.0 — correctness and docs

Date: 2026-09-26
Follows: v0.19.0 (local stack and API polish)

## Included (Steps 194–239)

- API correctness: replay Location headers, envelope errors both services,
  contract/service versions in parity.
- Docs refreshed to reality: operator guide, runbook, handover, risks,
  acceptance, evidence, readme status, release↔tag parity both directions.
- Process: rolling branch, verify-before-merge, sync-after-release rules.

## Verification

`python -m pytest -q` → 171 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema + seam ready, driver pending); compose never run here;
  R3 excluded.

## Tag

`v0.20.0` on `main`. Create the GitHub Release from this tag in the web UI.
