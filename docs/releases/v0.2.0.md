# Release v0.2.0 — governance + deployment start

Date: 2026-09-24
Follows: v0.1.0 (R1 skeleton + R2 policy slices)

## Included (Steps 28–30)

- Change-control record with v0.1.0 entries (`docs/change-control.md`).
- Post-release risk review, reserve untouched (`docs/risks.md`).
- Kubernetes start: namespace, control-api deployment (probes, CPU/mem bounds, `:v0.1.0`), service (`infra/k8s/`, `tests/test_k8s.py`).

## Verification

`python -m pytest -q` → 65 passed. All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Same as v0.1.0, plus: k8s manifests unapplied (no cluster in CI); telemetry stack manifests pending (Step 31).

## Tag

`v0.2.0` on `main`. Create the GitHub Release from this tag in the web UI.
