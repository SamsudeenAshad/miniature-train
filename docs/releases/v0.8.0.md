# Release v0.8.0 — full platform manifests

Date: 2026-09-25
Follows: v0.7.0 (HTTP services and console)

## Included (Steps 54–106)

- Workloads: inference (2 replicas, PDB, dedicated identity), console (image, deploy, proxy, config), Postgres, MinIO, MLflow, OTel collector, Prometheus (+rules/config), Grafana (+datasources, dashboards), Loki, Tempo, Alertmanager.
- Delivery: Rollout bound to inference with Istio routing, stable/canary services, guard analysis, Argo CD app with field ownership, training WorkflowTemplate under tokenless SA, sync waves.
- Security: scoped SAs/RBAC (inference, executor), ExternalSecrets, default-deny NetworkPolicy with service-covering egress, tokenless/seccomp/bounded fleet with enforcing tests, pinned images (swept).
- Data: bucket job, backup CronJob with real CLI, retention tombstones, PDBs, quotas, LimitRange.
- Process: branch practices, CI with web checks + build.

## Verification

`python -m pytest -q` → 130 passed; `npm run check` + `build` green.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores; R3 excluded.

## Tag

`v0.8.0` on `main`. Create the GitHub Release from this tag in the web UI.
