# Release v0.14.0 — hardening sweeps and persistence schema

Date: 2026-09-26
Follows: v0.13.0 (persistence path and local dev)

## Included (Steps 135–179)

- Disruption budgets across inference, API, and stateful singletons.
- Strict typecheck in CI; console backend status; UI checks hardened.
- Migrations 001–010 with parity tests; storage seam; quota/runbook alignment.
- Sweeps extended everywhere: seccomp, tokens, resources, probes, selectors,
  ConfigMaps, secrets, volumes, images, workflows included.
- Governance current: risks, acceptance, change log, evidence, runbooks.

## Verification

`python -m pytest -q` → 162 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema ready, driver pending); R3 excluded.

## Tag

`v0.14.0` on `main`. Create the GitHub Release from this tag in the web UI.
