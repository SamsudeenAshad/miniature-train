# Release v0.15.0 — governance refresh and local-dev hardening

Date: 2026-09-26
Follows: v0.14.0 (hardening sweeps and persistence schema)

## Included (Steps 164–189)

- Governance: risks, acceptance, change log, evidence, runbook current.
- Compose: real Dockerfiles, healthy DB gating, console with proxied backends,
  healthchecks everywhere, healthy-only dependencies, restart policies, docs.
- Runbook covers compose entries, migrations, budgets, sync waves.

## Verification

`python -m pytest -q` → 162 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema ready, driver pending); compose never run here; R3 excluded.

## Tag

`v0.15.0` on `main`. Create the GitHub Release from this tag in the web UI.
