# Release v0.24.0 — docs true and API locked down

Date: 2026-09-27
Follows: v0.23.0 (docs and frontend rigor)

## Included (Steps 224–279)

- Docs describe reality: handover, operator guide, runbook, readme status.
- API: anonymous and non-admin callers denied and tested; request IDs always present.
- Console: config fallback units, screen coverage, proxy parity.

## Verification

`python -m pytest -q` → 181 passed; `npm test`/`typecheck`/`build`, `pip-audit`/`npm audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema + seam ready, driver pending); compose never run here;
  R3 excluded.

## Tag

`v0.24.0` on `main`. Create the GitHub Release from this tag in the web UI.
