# Release v0.17.0 — API correctness

Date: 2026-09-26
Follows: v0.16.0 (contracts honed)

## Included (Steps 194–209)

- Correct 200/201 replay semantics with Location headers, documented in OpenAPI.
- Predictor loads once; readiness and overview report real backend state.
- Private-store reaches removed; idempotency attaches through public API.

## Verification

`python -m pytest -q` → 166 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema ready, driver pending); compose never run here; R3 excluded.

## Tag

`v0.17.0` on `main`. Create the GitHub Release from this tag in the web UI.
