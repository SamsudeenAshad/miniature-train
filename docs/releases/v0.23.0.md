# Release v0.23.0 — docs and frontend rigor

Date: 2026-09-26
Follows: v0.22.0 (follow-through)

## Included (Steps 224–269)

- Docs describe the real system: handover, operator guide, runbook, evidence,
  risks, acceptance, readme status, release↔tag parity.
- Frontend: strict typecheck, runnable unit tests, screen coverage, proxy configs.
- Backend: envelope errors, Location headers, version parity, storage seams.

## Verification

`python -m pytest -q` → 180 passed; `npm test`/`typecheck`/`build`, `pip-audit`/`npm audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema + seam ready, driver pending); compose never run here;
  R3 excluded.

## Tag

`v0.23.0` on `main`. Create the GitHub Release from this tag in the web UI.
