# Release v0.13.0 — persistence path and local dev

Date: 2026-09-26
Follows: v0.12.0 (seams and supply chain)

## Included (Steps 131–169)

- Migrations 001–010: projects, idempotency, audit, outbox, incidents, deployments,
  actions, lineage, predictions, AIOps — gapless, indexed, quota/runbook aligned.
- Storage seam in the control API; runbook and evidence refreshed.
- Local compose: real Dockerfiles, healthy DB gating, console with proxied backends.
- Console: version tracking, backend status, dual proxy configs under check.

## Verification

`python -m pytest -q` → 162 passed; `tsc`/`check`/`build`/`pip-audit` clean or noted.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; pilot singleton stores;
  in-memory API stores (schema ready, driver pending); R3 excluded.

## Tag

`v0.13.0` on `main`. Create the GitHub Release from this tag in the web UI.
