# Release v0.7.0 — HTTP services and console

Date: 2026-09-25
Follows: v0.6.0 (acceptance and governance close-out)

## Included (Steps 51–53 + branch practices)

- Control API: FastAPI projects + overview endpoints with HTTP tests.
- Inference: HTTP `/predict` with pinned model, `X-Request-ID` echo, 422 on invalid input.
- Console: React + Vite app with project/env context, 10 screens, approval/execution separation, `npm run check` + `npm run build` green.
- Process: `CONTRIBUTING.md` branch practices; Steps shipped as `feat/*` branches merged `--no-ff`.

## Verification

`python -m pytest -q` → 86 passed; `npm run check` + `npm run build` green.
All commits by `samsudeenashad`, no co-author trailers.

## Known limitations

- Manifests unapplied (no cluster in CI); perf/SLO unmeasured; R3 excluded.

## Tag

`v0.7.0` on `main`. Create the GitHub Release from this tag in the web UI.
